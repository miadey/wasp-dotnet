@{
    ViewData["Title"] = "Home";
}

<h1>Welcome</h1>
<p>Stock ASP.NET MVC running entirely inside an Internet Computer canister.</p>
<p>The view you're reading was rendered server-side by a controller action
inside <code>http_request_update</code>. No Node, no Docker, no edge function —
just the canister.</p>
