@* Single root component for HtmlRenderer-based SSR. Plain elements,
   minimal attributes — the renderer's RenderAttributes path NRE's on
   some attribute shapes under heavy trim. *@
<h1>RazorOnIc — Blazor SSR on ICP</h1>
<p>Counter value: @RazorOnIcCanister.GetCount()</p>
<p>This page was rendered server-side by HtmlRenderer inside a WebAssembly canister.</p>
