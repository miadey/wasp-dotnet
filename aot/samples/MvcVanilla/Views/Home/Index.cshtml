@{
    ViewData["Title"] = "Home";
}

<div>
    <h1>Welcome to MvcVanilla on ICP</h1>
    <p>This is the stock <code>dotnet new mvc</code> Index view, rendered server-side
       by the ASP.NET Core MVC view engine inside an Internet Computer canister.</p>
    <p>If you can read this, controller activation + view rendering + layout
       composition all work under NativeAOT-LLVM wasm32-wasi.</p>
</div>
